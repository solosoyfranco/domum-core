services:
  nodered:
    image: nodered/node-red:latest
    container_name: nodered
    restart: unless-stopped
    user: "1000:1000"
    networks:
      - domum-internal
      - domum-proxy
    environment:
      - TZ=America/New_York
    volumes:
      - nodered-data:/data
    labels:
      - traefik.enable=true
      - traefik.docker.network=domum-proxy
      - traefik.http.routers.nodered.rule=Host(`nodered.${DOMUM_DOMAIN}`)
      - traefik.http.routers.nodered.entrypoints=websecure
      - traefik.http.routers.nodered.tls=true
      - traefik.http.routers.nodered.tls.certresolver=cf
      - traefik.http.routers.nodered.middlewares=securityHeaders@file,forwardedHeaders@file
      - traefik.http.services.nodered.loadbalancer.server.port=1880
    profiles:
      - core

volumes:
  nodered-data:

networks:
  domum-internal:
    external: true
  domum-proxy:
    external: true
